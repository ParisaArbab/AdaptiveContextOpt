# Export these variables in your shell, or copy this file to .env for Compose.
MODEL=llama3.1:8b
CONTEXT_WINDOW=16384
GPU_DEVICE=0
# Bind paths should be absolute when the data live outside this project.
# INPUTS_DIR=/path/to/benchmark-json-files
# RESULTS_DIR=/path/to/results
# OLLAMA_MODELS_DIR=/path/to/copied-dot-ollama
# OLLAMA_IMAGE=ollama/ollama:0.33.3
