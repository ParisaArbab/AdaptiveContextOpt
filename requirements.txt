datasets>=2.19.0
graphifyy>=0.9.0
lean-ctx-sdk>=0.3.0
openai>=1.40.0
anthropic>=0.34.0
# Real token counting for the evaluation framework. Without it token_meter
# degrades to a chars/4 estimate and every report is tagged
# tokenizer_exact=false, which the analyzer surfaces rather than hides.
tiktoken>=0.7.0
# Figures produced by wp1/plot_results.py at the end of every run.
matplotlib>=3.7.0
# Ochiai SBFL needs per-test line coverage; without it the SBFL column of
# FlexFL's candidate merge is unavailable and the merge says so explicitly.
coverage>=7.0.0
# Optional: exact token counts for a locally-served open-weight model, using
# that model's own tokenizer instead of tiktoken's approximation.
# transformers>=4.44.0
